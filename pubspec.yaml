name: pdf_design_system
description: Design system for PDF Flutter App
version: 0.1.0
homepage: https://github.com/Robespra/pdf_design_system.git

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  # No assets or fonts to include yet